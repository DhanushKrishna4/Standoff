{
  "name": "standoff",
  "version": "1.0.0",
  "private": true,
  "description": "A fair, explainable group movie-picker. Zero runtime dependencies — this manifest exists only for the Playwright end-to-end tests.",
  "scripts": {
    "test": "node --test",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
